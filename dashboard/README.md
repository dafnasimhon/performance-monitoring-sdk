# PerfSDK Dashboard

React dashboard for the PerfSDK Android performance monitoring SDK.

## Setup

```bash
npm install
npm run dev
```

Runs at `http://localhost:5173`. Requires the FastAPI backend running at `http://127.0.0.1:8000`.

## Stack

- Vite + React
- Recharts (charts)
- Axios (API calls)
